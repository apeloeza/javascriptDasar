alert = menampilkan form di html (popup)
loop = teknik berulang (i++)
coment = ctrl + /
prompt = memasukkan nilai sesuatu /sebuah inputan dari alert(pop up)
const = variabel yang tidak dapat dirubah berbeda dengan var & let
var & let = dapat ditimpa ke data yang baru
array = 
index = urutan
shift = menghapus awal
pop menghapus akhir
length = jumlah data dari sebuah array
while => mencari kondisi dahulu baru eksekusi
do => mencari eksekusi 1x baru cek kondisi
includes = mengetahui apakah array mengandung nilai (true/false)
indexof() = nilai array ke ()
contat = menggabungkan dua atau lebih array menjadi satu array baru
map = untuk membuat array baru dari memanggil sebuah function pada setiap elemen array
short = digunakan untuk menyortir array objek menggunakan fungsi bandingkan
filter = mencari semua elemen di dalam array yang sesuai di kriteria tertentu
append = metode yang ada dalam objek Document pada JavaScript
querySelector = akan mengambil satu elemen saja
looping ===========
for(statement 1 // statement2 // statement3)
statement 1 = nilai awal dari variabel
statement 2 = condition
statement 3 = Effect (a++)
========= Asyncronus =============
membuat kita dapat mengantri sesuai urutan 